import type {SemanticSceneProps} from "./types";import {GREEN,PAPER,ratio,sceneClaim} from "./shared";
export function AnimatedStatementScene(x:SemanticSceneProps){const r=ratio();return <div data-semantic-blueprint="animated-statement" style={{minHeight:520,display:"grid",placeItems:"center",padding:"30px 80px",textAlign:"center"}}><div><div style={{fontSize:76,lineHeight:1.04,fontWeight:920,color:PAPER,transform:`translateY(${(1-Math.min(1,r/.18))*30}px)`,opacity:Math.min(1,r/.12)}}>{sceneClaim(x)}</div><div style={{height:8,width:`${Math.min(1,r/.55)*620}px`,maxWidth:"70vw",margin:"28px auto 0",borderRadius:8,background:GREEN}}/></div></div>}
