You are creating the stable visual ontology for one long-form explainer.

Script:
```json
{{script}}
```

Return `episode_visual_model@1.0.0`.

Create one canonical entity for each concrete object, scientific subject, spatial region, persistent quantity set, or recurring abstract subject that must remain recognizable across scenes. Use stable kebab-case `entity_id` values. Put wording variants in `aliases`; do not duplicate entities for capitalization, plurality, or paraphrase.

Depictions describe what the semantic renderer should draw, not an icon-search query. Prefer physical or domain kinds. Use `abstract-subject` only when no honest physical/domain depiction exists. For molecules specify formula and geometry when known; for lattices specify repeated geometry; for physical objects specify shape/material when useful.

Keep appearance concise, reusable, topic-specific, and in the shared navy editorial visual world. Use one stable hex color per entity. Never add decorative nodes, cards, arrows, circles, or icons as entities.
