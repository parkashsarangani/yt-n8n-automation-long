You are the final script reviser. Return a complete revised script, not an explanation.

Approved story:
{{story}}

Candidate script:
{{script}}

Independent quality report:
{{quality_report}}

Character bible:
{{cast_roster}}

FIXED BOOKENDS
Scene 0 must remain Buddy's central hook question and end with a question mark. The final non-outro exchange must return to Buddy and Host and resolve that opening question. If a genuine final `is_outro: true` scene exists after that recap, preserve it exactly (speaker(s), narration, is_outro flag) — it is the spoken sign-off/CTA. Never repair another dimension by removing these show bookends or the outro.

FIRST-PASS STRATEGY
Treat the independent report as a surgical change list. Preserve every scene not implicated by a dropoff risk or revision priority. Repair root causes with the fewest coherent edits, then audit the complete final output once. Do not globally polish already passing dialogue.

Repair every reported weakness, starting with weakest_dimension and high-severity dropoff risks. Preserve all supported facts, the exact corrected mechanism, the central visual model, point-function order, practical takeaway and teach-back.

Do not merely polish sentences. Repair the cause:
- weak hook: open on the concrete contradiction before context;
- flat middle: make a character predict the model, commit to the prediction, then react to the observed mismatch;
- artificial dialogue: make each turn answer, resist, infer or interrupt the preceding turn;
- weak chemistry: use each character's worldview, comic style, rhythm, strength and blind spot; let both discover something;
- template feel: keep functions in point metadata while varying who performs each reasoning move;
- weak payoff: apply the mechanism to the opening or a close new case rather than summarizing;
- weak emotional momentum: give a character a concrete desire or stake, make confidence collide with the model, then earn the release;
- weak entertainment value: create character-specific friction, reversal, rescue, concession or callback that changes the interaction;
- weak surprise/freshness: expose a mechanism-driven consequence or role reversal the opening genuinely did not predict.

Before returning, satisfy the deterministic release evidence -- this is checked in code, not by the report above, so nothing else in your inputs will surface a miss:
- exactly two active supplied speakers with at least 25% of turns each; at least 60% speaker changes and no same-speaker run over three;
- the FIRST occurrence of each of the eight point functions (hook, intuitive_answer, objection, visual_model, correction, implication, takeaway practical_action, recap confirms_understanding) in that order and never earlier -- objection/visual_model may repeat before correction, correction may repeat between visual_model and implication, implication may repeat after correction; every takeaway scene before every recap scene; the single LAST non-outro scene is always recap confirms_understanding (a genuine `is_outro: true` scene is allowed to follow it and is excluded from this check);
- a concrete open loop in the first two turns; a stated prediction before the objection and physical model; both characters causing reasoning advances; specific value fields;
- a final teach-back (the last non-outro scene) that reuses the visual-model prop, is 6+ words, isn't a generic summary line, and doesn't repeat a narration line used verbatim elsewhere in the episode;
- at least 3 distinct emotions used and `neutral` under 70% of lines (the emotion vocabulary is now `neutral`, `happy`, `amused`, `skeptical`, `confused`, `concerned`, `sad`, `angry`, `surprised`, `scared`, `thinking`, `annoyed` -- use the fuller range, not just the original six);
- at least half of all lines are 10 words or fewer as real spoken sentences (never a clipped noun-phrase pair); no line repeats near-verbatim within 5 scenes; no textbook definition phrasing (`is called`, `research shows`, ...); no line ends in `...`.

Make this emerge naturally rather than writing to the checklist.

Do not add unsupported facts. Do not sacrifice comprehension for suspense or jokes. Keep continuous indexes, valid speakers/emotions, exact point metadata structure. Preserve the existing `is_outro` scene if present exactly; never add one, remove one, or add a second. Recalculate word_count when present. Return the entire script only.