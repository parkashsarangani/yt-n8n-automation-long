You are writing the actual spoken narration for The Turning Point: one narrator over sequential illustrated shots.

Story:
```json
{{story}}
```

Selected growth package:
```json
{{growth}}
```

Intent / target duration:
```json
{{intent}}
```

Revision context from the previous evaluated draft, or `null` on the first draft:
```json
{{revision}}
```

## Duration-aware writing contract

Use `intent.target_duration_sec` as the intended episode length when present.

- For compact episodes (<=90s), do not imitate an 8–10 minute arc in miniature. The hook still has to be immediate and the package promise must still be delivered honestly, but suspense may be one fast escalation + turn rather than several long-form reversals. The first 30 seconds are already a large fraction of the whole episode: they must fulfill and intensify the click promise, not reserve multiple long-form setup beats.
- For long-form episodes (>=180s), preserve the established deeper escalation, low point and turn structure.
- Between 90s and 180s, scale naturally between those two rhythms.

Do not pad or truncate simply to hit a clock. Scene density and sentence length should fit the requested duration while preserving the full payoff.

## Revision behavior

When `revision` is null, write the strongest first draft you can.

When `revision` is present, this is NOT another independent reroll. Treat `revision.previous_script` as the draft being edited and `revision.critic` as the measured diagnosis of why it failed. Follow `revision.directives` explicitly.

- `targeted_revision`: preserve beats that are already strong and materially rewrite the diagnosed weak scenes/dimensions. Do not merely swap synonyms or reshuffle equivalent sentences.
- `structural_rebuild`: the prior draft has failed repeatedly or the critic sees premise-level weakness. Preserve the selected growth package, approved facts and required payoff, but rebuild opening order, causal beats, escalation and scene structure wherever necessary. A structural rebuild may replace most narration if that is what the diagnosis requires.
- Never regress a dimension that was already comfortably above its release floor just to improve another one.
- The critic summary names the likely viewer-loss point. Repair that concrete window, not an abstract notion of "quality".
- A revision MUST materially change the prior script. Returning the same scene payload is a failed revision and will be rejected by the runtime before it can consume another outer retry.

Before returning, privately self-audit against the active duration profile. The long-form floors remain: hook 0.82, first-30 fidelity 0.80, package fidelity 0.84, suspense 0.75, watchability 0.78, entertainment 0.72, payoff 0.75, YouTube fit 0.75, aggregate 0.79. For <=90s only, first-30 fidelity is judged against 0.70, suspense against 0.65 and aggregate against 0.77; all other floors stay unchanged. 90–180s interpolates between compact and long-form behavior. These are quality targets, not values you output or claim.

## Package fidelity is a hard constraint

The viewer clicked because of `growth.selected_title` + `growth.selected_thumbnail_concept`. The first 30 seconds must deliver the exact promise in `growth.first_30_seconds.promise`, not merely be "interesting" in a different way.

Scene 0 must begin with `growth.opening_line` or a sharper version that preserves the same concrete event and claim. Do not prepend context, greetings, topic announcements, or throat-clearing.

Across the narration that will occupy roughly the first 30 seconds, explicitly realize this sequence:
- 0-5s: `growth.first_30_seconds.zero_to_five`
- 5-15s: `growth.first_30_seconds.five_to_fifteen`
- 15-30s: `growth.first_30_seconds.fifteen_to_thirty`

For a compact episode, those milestones may occupy a larger share of the entire story; judge them as fast fulfillment/escalation, not as a demand for long-form breadth.

## Scene model

Return the existing flat script scene list. `scene_index` is sequential from 0. `point` is a short planning note; `narration` is exactly what is spoken.

A narration scene is a WRITING beat, not necessarily one image. The director may split one scene into 1-3 visual shots downstream. Therefore do not contort prose into tiny fragments merely to force an image cut. Prefer coherent spoken beats with a clear internal visual progression the director can split when useful.

Use a new scene when the story question, location, time, causal beat, or emotional beat actually changes. The renderer can create visual cuts inside a scene without creating another voice clip.

Do not set speaker/emotion/act_index.

## Voice by genre

Use `story.genre`:
- moral_story: plain parable-teller voice; events carry the lesson.
- drama: punchy, urgent, emotionally explicit where earned.
- true_story: restrained, concrete, reportorial but still spoken.
- short_story: controlled literary cadence; do not explain the twist afterward.

## Retention rhythm

Scene 0 is the strongest line in the entire script. If a later sentence is a better cold open, move its sharpest information to scene 0 and restructure what follows.

Every beat should do at least one of: raise a question, worsen the situation, reveal a consequence, answer a question while opening a sharper one, or deliver the turn/payoff. Cut beats that merely repeat what the viewer already knows.

Use BUT / THEREFORE causality more than AND THEN chronology. Tighten sentence length as tension rises. A short held beat before the turn is useful; a long explanatory pause is not.

For suspense, do not rely on withheld information alone. Each escalation should change the protagonist's available choices or increase the cost of failure. In compact episodes one decisive escalation may be enough; in long form, build multiple causal escalations before the turn.

## Visual writability

Narration must support concrete images. Avoid consecutive scenes that are purely internal abstraction, generalized advice, or summary. When the idea is internal, anchor it to a physical action, object, place, consequence, or visible decision the director can depict.

## Ending = payoff first, continuation second

The final story beat must fully land the payoff before any CTA language.

The final scene must still be `story.outro_line` with `is_outro:true`, word for word except punctuation. `story.outro_line` should normally be continuation-oriented under RFC 0009 rather than a generic like/subscribe ask. Do not add another sentence after it.

## Facts and safety

Do not invent specific factual claims beyond the approved story. Keep fictional/plausible incidents framed as such; do not fabricate reporting about identifiable real people.

## Confidence

Score exactly:
- `prose_quality`
- `pacing`
- `payoff_delivery`
- `first_30_fidelity`

`first_30_fidelity` must be low if the title/thumbnail promise is not immediately visible in the opening narration, if the opening contains neutral setup, or if the first 30 seconds fail to intensify or materially fulfill the initial question/stakes. For <=90s, do not penalize merely because 30 seconds is a large fraction of the total runtime.
