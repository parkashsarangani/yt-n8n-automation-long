{
  "schema_id": "story",
  "version": "1.4.0",
  "status": "active",
  "description": "Adds optional genre, echoed verbatim from the run's intent.genre by narrative_story_architect. Carrying it on the story artifact (rather than requiring every downstream consumer to also depend on intent) lets narration_script_writer and watchability_critic read it for free, since they already consume the full story object. Additive per RFC 0007: existing 1.3.0 artifacts remain valid, no migrator needed.",
  "produced_by": [
    "story_architect",
    "narrative_story_architect",
    "human"
  ],
  "json_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "StoryArtifact",
    "type": "object",
    "additionalProperties": false,
    "required": [
      "topic",
      "title",
      "hook",
      "acts",
      "payoff",
      "outro_line"
    ],
    "properties": {
      "topic": {
        "type": "string",
        "minLength": 8,
        "maxLength": 300
      },
      "title": {
        "type": "string",
        "minLength": 5,
        "maxLength": 100
      },
      "hook": {
        "type": "string",
        "minLength": 10,
        "maxLength": 600
      },
      "genre": {
        "type": "string",
        "enum": ["moral_story", "drama", "true_story", "short_story"],
        "description": "Echoed from the run's intent.genre (defaults to moral_story when the operator didn't choose one). Downstream narration and critique prompts read this to match voice and structure to the chosen content type."
      },
      "acts": {
        "type": "array",
        "minItems": 3,
        "maxItems": 6,
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "act_index",
            "act_title",
            "premise",
            "target_words"
          ],
          "properties": {
            "act_index": {
              "type": "integer",
              "minimum": 0
            },
            "act_title": {
              "type": "string",
              "minLength": 3,
              "maxLength": 120
            },
            "premise": {
              "type": "string",
              "minLength": 20,
              "maxLength": 1200
            },
            "target_words": {
              "type": "integer",
              "minimum": 50,
              "maximum": 1200
            }
          }
        }
      },
      "payoff": {
        "type": "string",
        "minLength": 10,
        "maxLength": 800
      },
      "comment_hook": {
        "type": "string",
        "maxLength": 300
      },
      "outro_line": {
        "type": "string",
        "minLength": 5,
        "maxLength": 300
      },
      "seo_description": {
        "type": "string",
        "maxLength": 4500,
        "description": "DEPRECATED. Superseded by the seo_metadata artifact, which is written from the finished script rather than the premise. Kept so story@1.1.0 artifacts written before the SEO agent existed stay valid; the story prompt no longer asks for it and publish no longer reads it."
      },
      "tags": {
        "type": "array",
        "minItems": 5,
        "maxItems": 20,
        "items": {
          "type": "string",
          "minLength": 2,
          "maxLength": 60
        },
        "description": "DEPRECATED. Superseded by seo_metadata.tags. Retained for artifacts written before the SEO agent existed. Note these permitted 20 tags of 60 chars — up to 1200 characters — which exceeds YouTube's 500-character aggregate tag budget; seo_metadata is bounded to fit."
      },
      "retention_beats": {
        "type": "array",
        "maxItems": 20,
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "at_fraction",
            "device"
          ],
          "properties": {
            "at_fraction": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "device": {
              "type": "string",
              "maxLength": 300
            }
          }
        }
      }
    }
  }
}
